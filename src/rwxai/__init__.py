"""Verification package for the residual-weight explainable IDS manuscript.

The modules copied from the experiment server keep their original names and
contents so their SHA-256 values still match `server_snapshot/v4`. New code for
offline verification lives in `verify.py`, `tables.py` and `evidence.py`.
"""

__version__ = "1.0.0"
